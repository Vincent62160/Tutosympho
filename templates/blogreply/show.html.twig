{% extends 'base.html.twig' %}

{% block title %}Blogreply{% endblock %}

{% block body %}
    <h1>Blogreply</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blogreply.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ blogreply.name }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ blogreply.message }}</td>
            </tr>
            <tr>
                <th>Datetime</th>
                <td>{{ blogreply.datetime ? blogreply.datetime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('blogreply_index') }}">back to list</a>

    <a href="{{ path('blogreply_edit', {'id': blogreply.id}) }}">edit</a>

    {{ include('blogreply/_delete_form.html.twig') }}
{% endblock %}
